// You can add any JavaScript functionalities here if needed
// For example, dynamically changing the model source, handling events, etc.

document.addEventListener('DOMContentLoaded', () => {
    const modelViewer = document.getElementById('myModel');
    
    // Example: Change model source dynamically (optional)
    // modelViewer.src = 'new_model_path.gltf'; 
});